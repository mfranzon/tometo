# Codex

Native child: a Codex subagent, when multi-agent is on and the role's runner is `codex` with no model or effort pin.

Pinned model, pinned effort, or any other runner: `bin/tometo spawn`. That process is local.
