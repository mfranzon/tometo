# Claude Code

Native child: the Agent tool, agent `tometo-agent`, when the role's runner is `claude` and the role does not pin `model` or `effort`.

Pinned model, pinned effort, or any other runner: `bin/tometo spawn`.

A child that writes code gets its own git worktree.
