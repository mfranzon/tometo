# Cursor

Native child: the Task tool, `subagent_type` `tometo-agent`, `environment` `local`, when the role's runner is a Cursor model slug.

Any other runner, including a local `grok` or `claude` binary: `bin/tometo spawn`.
