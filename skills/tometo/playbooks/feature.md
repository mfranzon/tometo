# Feature

1. Name the data shape, and name what the user can do after this that they could not do before.
2. Implement the smallest version of that shape.
3. Drive it the way a user does. Record the command and what happened.
4. When the user asked to ship, continue with `opening-a-pr.md`.
